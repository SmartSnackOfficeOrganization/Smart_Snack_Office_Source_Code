"""Paquete de modelos de IA/ML del proyecto (Scikit-learn).

Según la arquitectura del proyecto (CLAUDE.md), los modelos de Scikit-learn
viven en la carpeta ``ml/`` (raíz del repositorio), separados del backend
Django en ``Backend/``.
"""
